"""Setup script for affordance probing project."""

from setuptools import setup, find_packages

setup(
    name="affordance-probing",
    version="0.1.0",
    description="Geometric Affordance Probing Across VLA Encoders",
    packages=find_packages(),
    python_requires=">=3.11",
)
